extends base

block content
    form(method="post")
        input(type="text", name="title",placeholder="Input title",required,maxlength="20")
        textarea(name="content", cols="30", rows="10",required,id="notice",placeholder="input write notice ") 
        input(type="submit",value="submit")