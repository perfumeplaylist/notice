extends base

block content
    h1=pageTitle
    form(method="post")
        input(type="text", name="title",placeholder="Input title",required)
        textarea(name="content", cols="30", rows="10",required,id="notice",placeholder="input write notice ") 
        input(type="submit",value="submit")